/* eslint-disable no-unused-vars */

export enum SpringType {
  FADE = 'fade',
  SLIDE_UP = 'slideUp',
  SLIDE_LEFT = 'slideLeft',
  ZOOM = 'zoom'
}

export enum AnchorType {
  TOP = 'top',
  RIGHT = 'right',
  BOTTOM = 'bottom',
  LEFT = 'left'
}
